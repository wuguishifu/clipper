{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "deep-link:default",
    "store:default",
    "shell:allow-execute",
    "shell:allow-kill",
    "dialog:default",
    "dialog:allow-open"
  ]
}